import 'package:flutter/material.dart';

const labelTextStyle = TextStyle(fontSize: 18, color: inActiveColor);
const numberStyle =
    TextStyle(fontSize: 50, fontWeight: FontWeight.w900, color: Colors.white);

const inActiveColor = Color.fromRGBO(130, 131, 141, 1);
const activeColor = Colors.white;
const bottomButtonColor = Color.fromRGBO(232, 21, 75, 1);
